
<% if(mode === 'identity') { %>
    <p> galleryId (필수): 
        <input type="text" id="galleryId">
    </p>

    <p> 동작 타입:
        <select id="action-type">
            <option value="insert" >
                삽입
            </option>
            <option value="delete" >
                비교
            </option>
        </select>
    </p>

    <p> 검색 타입:
        <select id="search-type">
            <option value="search_all" 
                <%= type === 'search_all' ? 'selected' : '' %>
            >
                전체
            </option>
            <option value="search_name" 
                <%= type === 'search_name' ? 'selected' : '' %>
            >
                글쓴이
            </option>
            <option value="search_subject_memo" 
                <%= type === 'search_subject_memo' ? 'selected' : '' %>
            >
                제목+내용
            </option>
        </select>
    </p>

    <p> 닉네임: 
        <input type="text" id="nickname" value="ㅇㅇ" 
            <%= type !== 'search_name' ? 'disabled' : '' %>
        >
        키워드 (검색어): 
        <input type="text" id="keyword" value="릿" 
            <%= type !== 'search_subject_memo' ? 'disabled' : '' %>
        >
    </p>
    <p> UID (식별코드): 
        <input type="text" id="uid" disabled>
    </p>
    <p> pos (URL의 search_pos 값(양수)) 또는 시작 페이지: 
        <input type="number" id="pos" step="10000" value="1">
    </p>

    <p> 단위:
        <select id="unit-type">
            <option value="page">
                페이지
            </option>
            <option value="position"
                <%= type === 'search_all' ? 'disabled' : '' %>
            >
                포지션
            </option>
        </select>
    </p>
<% } %>